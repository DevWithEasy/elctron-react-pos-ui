{
  "name": "pos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"electron .\" \"cd src && npm run dev\"",
    "build": "cd src && npm run build && cd .. && election-builder"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0",
    "electron": "^23.2.0",
    "electron-builder": "^23.6.0"
  },
  "dependencies": {
    "electron-is-dev": "^2.0.0"
  }
}
